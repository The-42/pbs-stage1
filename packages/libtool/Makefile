package = libtool
version = 2.4.6

location = https://ftp.gnu.org/pub/gnu/$(package)
tarballs = $(package)-$(version).tar.xz

patches = \
	ltmain-sysroot.patch \
	disable-manpage-generation.patch

include packages/all-package.mk

